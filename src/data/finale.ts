/**
 * Finale closing message — placeholder per D-015, user replaces (D-011).
 * `message` renders as display type; `line` as the single serif italic line.
 */
export const finale = {
  message: "Esto es solo el principio.",
  line: "Nuestra historia recién comienza.",
};